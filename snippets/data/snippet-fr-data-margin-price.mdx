  <Accordion icon="input-numeric" title="Margin price (MAR)">
    <ResponseField name="margin price" type="decimal">
      Le `margin price` est le prix auquel vous devez vendre l'article acheté au `purchase price` pour obtenir un `taux de marge` défini dans les [paramètres](/lg-french/settings).
      <p>$margin\,price = purchase\,price * (1+margin)$</p>
    </ResponseField>
  </Accordion>